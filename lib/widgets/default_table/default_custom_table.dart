import 'package:flutter/material.dart';

class DefaultCustomTable extends StatelessWidget {
  const DefaultCustomTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
